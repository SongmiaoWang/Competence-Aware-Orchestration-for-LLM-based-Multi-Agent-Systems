# Competence-Aware Orchestration (code excerpt)

This directory is intended for reviewing network definitions and how the three stages connect.

## Layout

- **`competence_orchestration/model/`** — module definitions  
  - Task-Conditioned Competence Modeling (`TaskConditionedCompetenceModel`)  
  - Role Activation (`RoleActivationModel`, `RoleActivationPipeline`)

- **`competence_orchestration/graph/`** — graph head for Graph-based MAS Generation (`GraphBasedMASGenerationHead`)

## Full release

Reproducible code, data, and run instructions will be released later.
